# Full Project 完整启动流程

> 这是 `triple_imu_rs485_publisher_dual_cam_UI_voice.py` 的模块化重构版本

## 📁 项目结构

```
full_project/
├── main.py                 # 主程序入口
├── config/
│   └── settings.yaml       # 所有配置参数
├── core/
│   ├── imu_handler.py      # IMU数据处理
│   ├── kinematics.py       # 运动学计算
│   ├── gripper.py          # 夹爪控制
│   ├── zmq_manager.py      # ZMQ通信管理
│   ├── video_receiver.py   # 视频接收
│   └── audio_receiver.py   # 音频接收
├── ui/
│   ├── viewer.py           # PyQt5可视化界面
│   └── widgets/            # UI组件
├── scripts/
│   └── setup_tunnel.sh     # SSH隧道建立脚本
└── keys/                   # SSH密钥文件
```

## 🔌 端口架构 (与 triple 一致)

| 端口 | Socket类型 | 方向 | 用途 |
|------|-----------|------|------|
| 5555 | PUSH | A → B | 控制数据发送 |
| 5557 | SUB | B → A | 视频流接收 |
| 5559 | PUSH | A → LeRobot | LeRobot数据 |
| 5560 | PUB | 主程序 → UI | 调试数据发布 |
| 5561 | SUB | B → A | 音频流接收 |
| 5562 | PULL | UI → 主程序 | UI命令接收 |

---

## 1️⃣ 部署B端（仅首次需要）

```bash
cd ~/桌面/stable_wit_485/imu_RS485/whole3_2
./deploy_to_target_server.sh
```

---

## 2️⃣ 终端1：SSH到服务器并建立隧道 + 运行B

⚠️ **重要：这个终端需要保持打开，不要关闭！**

### 方法A：使用脚本（后台隧道）

```bash
cd ~/桌面/stable_wit_485/imu_RS485/full_project
./scripts/setup_tunnel.sh

# 然后单独登录服务器运行 B
cd ~/桌面/stable_wit_485/imu_RS485/whole3_2
./connect.sh
# 在服务器上执行：
conda activate lerobot_server
cd /data7/yhx/lerobot_data_collection
./start_b_voice.sh
```

### 方法B：手动建立隧道（推荐，可以同时登录）

```bash
cd ~/桌面/stable_wit_485/imu_RS485/whole3_2

# 建立 SSH 隧道并登录服务器（6个端口）
# 新跳板机: 202.112.113.15:2228 (scorpio_jump_2228)
ssh -i ./capri_yhx_2237 -p 2237 \
    -o "ProxyCommand ssh -i ./scorpio_jump_2228.txt -p 2228 -W %h:%p root@202.112.113.15" \
    -L 5555:localhost:5555 \
    -L 5556:localhost:5556 \
    -L 5557:localhost:5557 \
    -L 5558:localhost:5558 \
    -L 5559:localhost:5559 \
    -L 5561:localhost:5561 \
    root@202.112.113.74

# 登录后，在服务器上执行：
conda activate lerobot_server
cd /data7/yhx/lerobot_data_collection
./start_b_voice.sh
```

---

## 3️⃣ 终端2：验证隧道 + 运行主程序（本地）

```bash
# 验证 SSH 隧道是否建立（应该看到 6 个端口）
ss -tuln | grep -E "5555|5556|5557|5558|5559|5561"

# 运行主程序
cd ~/桌面/stable_wit_485/imu_RS485/full_project
conda activate lerobot
python main.py
```

### 主程序终端控制

运行后可以使用键盘控制：
- **按住 `1`** → 打开夹爪
- **按住 `2`** → 关闭夹爪
- **`q`** → 退出程序

---

## 4️⃣ 终端3：运行C端（本地，可选）

```bash
cd ~/桌面/stable_wit_485/imu_RS485/whole3_2
python C_real_video_audio.py
```

---

## 5️⃣ 终端4：运行PyQt5 UI（本地，可选）

```bash
cd ~/桌面/stable_wit_485/imu_RS485/full_project/ui
conda activate pyqt5_ui
python viewer.py
```

### UI功能

- ✅ 实时显示IMU数据（3个传感器姿态角）
- ✅ 3D轨迹可视化
- ✅ 双摄像头视频流显示
- ✅ 夹爪控制（按钮或键盘 1/2）
- ✅ 音频波形显示
- ✅ 在线状态监控

---

## ⚙️ 配置文件

所有参数都在 `config/settings.yaml` 中配置：

```yaml
# IMU配置
imu:
  serial_port: "/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0"
  baudrate: 9600
  addresses: [0x50, 0x51, 0x52]

# 网络配置
network:
  b_server:
    host: "localhost"
    port: 5555
  video:
    enabled: true
    port: 5557
  audio:
    enabled: true
    port: 5561
  debug_ui:
    enabled: true
    port: 5560
  ui_command:
    port: 5562

# 控制参数
control:
  publish_rate: 20
  online_only: true
```

---

## 🛠️ 故障排查

### 检查端口占用

```bash
# 检查隧道端口
ss -tuln | grep -E "5555|5556|5557|5558|5559|5561"

# 检查本地服务端口
lsof -i :5560  # 调试发布
lsof -i :5562  # UI命令
```

### 杀掉旧进程

```bash
# 杀掉旧的主程序
pkill -f "python main.py"

# 杀掉旧的SSH隧道
pkill -f "ssh.*5555"

# 杀掉UI
pkill -f "python viewer.py"
```

### 重启系统

```bash
# 1. 杀掉所有进程
pkill -f "python main.py"
pkill -f "python viewer.py"
pkill -f "ssh.*5555"

# 2. 重新建立隧道（终端1）
# 3. 重新运行主程序（终端2）
# 4. 重新运行UI（终端4）
```

---

## 📋 与 triple 文件对比

| 功能 | triple_imu_...voice.py | full_project |
|------|----------------------|--------------|
| 代码行数 | ~2000行 | 模块化多文件 |
| 配置方式 | 命令行参数 | YAML配置文件 |
| IMU通信 | device_model | device_model（相同）|
| ZMQ通信 | 内嵌代码 | zmq_manager.py |
| UI | 独立的pyqt5_viewer | 集成在ui/目录 |
| 端口 | 完全一致 | 完全一致 |
